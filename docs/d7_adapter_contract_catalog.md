# D7 Adapter Contract Catalog

All adapters default to fake or disabled mode. Production mode requires explicit environment flags. Without the listed production enable flags, adapters must not perform real outbound calls.

| adapter_name | owning_context | legacy_source | fake_mode_behavior | staging_mode_behavior | production_mode_behavior | idempotency_key | audit_log_required | timeout_policy | retry_policy | error_shape | side_effect_safety_flags | required_tests | production_enable_env_flags | rollback_behavior |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CloudStorageAdapter | `aicrm_next.integration_gateway` | cloud orchestrator media helpers | return fake object key and signed-url placeholder | write to staging bucket only when staging flag is enabled | upload to approved bucket only with explicit production flag | `tenant_id:sha256(content):purpose` | yes | connect 2s, total 10s | bounded exponential backoff, no retry on validation errors | `{ok:false,error_code,message,adapter,request_id,retryable}` | `cloud_upload_executed`; `external_call_executed` | fake upload, size/type guard, idempotency, error mapping | `AICRM_NEXT_ENABLE_CLOUD_STORAGE_UPLOAD=true` | disable flag and route media writes to legacy fallback |
| WeComMediaAdapter | `aicrm_next.integration_gateway` | image/miniprogram/attachment library domains | return fake `media_id` with TTL marker | use staging corp credentials and allowlisted files only | upload real temporary media only with explicit flag and audit | `tenant_id:library_id:content_hash` | yes | connect 2s, total 8s | retry transient 5xx only, max 2 | `{ok:false,error_code,message,adapter,request_id,retryable}` | `wecom_media_upload_executed`; `external_call_executed` | fake media, TTL refresh, size limit, mime validation | `AICRM_NEXT_ENABLE_WECOM_MEDIA_UPLOAD=true` | disable flag and use legacy media resolver |
| WeComTagAdapter | `aicrm_next.integration_gateway` | questionnaire tag write fallback | record fake tag intent only | staging corp tag allowlist only | write real tag only with explicit flag and target allowlist | `external_userid:tag_id:source_event_id` | yes | connect 2s, total 8s | retry transient API errors with idempotency key | `{ok:false,error_code,message,adapter,request_id,retryable}` | `wecom_tag_write_executed`; `external_call_executed` | fake tag, allowlist, duplicate tag, error mapping | `AICRM_NEXT_ENABLE_WECOM_TAG_WRITE=true` | disable flag and use legacy questionnaire tag fallback |
| WeComMessageDispatchAdapter | `aicrm_next.integration_gateway` | User Ops and automation dispatch fallback | write dispatch intent with fake delivery id | allowlisted recipients with fake mode default | real message dispatch only with explicit flag and approval window | `campaign_id:member_id:content_hash` | yes | connect 2s, total 12s | retry queued transient failures only | `{ok:false,error_code,message,adapter,request_id,retryable}` | `wecom_dispatch_executed`; `external_call_executed` | fake dispatch, allowlist, media count, queue idempotency | `AICRM_NEXT_ENABLE_WECOM_DISPATCH=true` | disable flag, pause workers, legacy fallback dispatch |
| WeChatOAuthAdapter | `aicrm_next.integration_gateway` | `public_questionnaire_oauth.py` | return signed fake openid/unionid identity | use sandbox or test-app callback URLs | real OAuth callback only with explicit flag and domain approval | `state:nonce:redirect_path` | yes | connect 2s, total 10s | no retry on callback exchange except transient token endpoint | `{ok:false,error_code,message,adapter,request_id,retryable}` | `real_oauth_executed`; `external_call_executed` | state validation, callback, replay, session mapping | `AICRM_NEXT_ENABLE_WECHAT_OAUTH=true` | disable flag and route OAuth to legacy fallback |
| WeChatPayAdapter | `aicrm_next.integration_gateway` | `wechat_pay.py`; `admin_wechat_pay.py` | return fake prepay/session id and fake notify envelope | provider sandbox signing and notify verification | real payment checkout/notify only with explicit provider flag | `order_no:amount:provider` | yes | connect 3s, total 15s | retry notify processing idempotently, no duplicate charge | `{ok:false,error_code,message,adapter,request_id,retryable}` | `payment_provider_called`; `checkout_executed`; `external_call_executed` | signing, notify idempotency, replay, reconciliation | `AICRM_NEXT_ENABLE_WECHAT_PAY=true` | disable flag and route checkout/notify to legacy payment |
| AlipayAdapter | `aicrm_next.integration_gateway` | `alipay_pay.py`; `admin_alipay_pay.py` | return fake WAP form and fake notify result | provider sandbox WAP/notify/return | real Alipay WAP/notify/return only with explicit flag | `order_no:amount:provider` | yes | connect 3s, total 15s | retry notify processing idempotently | `{ok:false,error_code,message,adapter,request_id,retryable}` | `payment_provider_called`; `checkout_executed`; `external_call_executed` | signing, notify, return, idempotency, reconciliation | `AICRM_NEXT_ENABLE_ALIPAY=true` | disable flag and route payment to legacy Alipay |
| OpenClawWebhookAdapter | `aicrm_next.integration_gateway` | `openclaw_service`; automation OpenClaw push | record fake push payload and response id | staging OpenClaw endpoint with allowlist | real OpenClaw push only with explicit flag and audit | `member_id:payload_hash:operation` | yes | connect 2s, total 10s | retry transient webhook failures, dead-letter otherwise | `{ok:false,error_code,message,adapter,request_id,retryable}` | `openclaw_push_executed`; `external_call_executed` | fake push, timeout, retry, dead-letter | `AICRM_NEXT_ENABLE_OPENCLAW_PUSH=true` | disable flag and use legacy OpenClaw fallback |
| ArchiveSyncAdapter | `aicrm_next.integration_gateway` | `archive.py` | import fixture messages into isolated store | shadow sync into staging tables | real archive sync only with explicit flag and cursor lock | `corp_id:cursor:batch_hash` | yes | connect 3s, total 30s | retry cursor batch only after dedupe check | `{ok:false,error_code,message,adapter,request_id,retryable}` | `archive_sync_executed`; `external_call_executed` | cursor, dedupe, no-overwrite, shadow parity | `AICRM_NEXT_ENABLE_ARCHIVE_SYNC=true` | disable flag and rely on legacy archive sync |
| ContactsSyncAdapter | `aicrm_next.integration_gateway` | `contacts.py` | import fixture contacts only | staging corp contact shadow sync | real contact sync only with explicit flag and merge guard | `corp_id:external_userid:sync_version` | yes | connect 3s, total 30s | retry page-level transient errors | `{ok:false,error_code,message,adapter,request_id,retryable}` | `contacts_sync_executed`; `external_call_executed` | fake sync, merge, dedupe, no-leak | `AICRM_NEXT_ENABLE_CONTACTS_SYNC=true` | disable flag and use legacy contacts sync |
| IdentityMappingAdapter | `aicrm_next.identity_contact` | `identity.py`; identity domain | resolve from fixture identity map | shadow resolver alongside legacy identity | real identity mapping writes only with explicit flag | `external_userid:identity_type:source` | yes | local 1s, external 5s | retry only external lookup, no duplicate mapping | `{ok:false,error_code,message,adapter,request_id,retryable}` | `identity_mapping_write_executed`; `external_call_executed` | resolver parity, no-leak, conflict handling | `AICRM_NEXT_ENABLE_IDENTITY_MAPPING_WRITE=true` | disable flag and use legacy resolver |
| AutomationRuntimeAdapter | `aicrm_next.automation_engine` | automation runtime/workflow/agent modules | run workflow in dry-run and record planned actions | staging workflow with fake external dispatch default | real runtime only with explicit workflow and dispatch flags | `workflow_id:member_id:step_id:run_id` | yes | step 10s, workflow 60s | retry idempotent steps only, dead-letter non-idempotent | `{ok:false,error_code,message,adapter,request_id,retryable}` | `workflow_runtime_executed`; `agent_runtime_executed`; `external_call_executed` | dry-run, state machine, idempotency, dispatch guard | `AICRM_NEXT_ENABLE_AUTOMATION_RUNTIME=true` | disable runtime flags, pause workers, legacy runtime fallback |

## Shared Contract Rules

- Every external adapter must return a stable error shape.
- Every adapter must emit an audit event before and after attempted side effects.
- Every production mode requires explicit environment enablement.
- Every staging mode must support fake or dry-run execution.
- Every rollback path is flag-disable first, then route back to legacy fallback.
