version: 1
status: manifest_only_no_runtime_change
notes:
  - AI-CRM Next is the default FastAPI modular monolith runtime.
  - This manifest documents ownership only; it does not narrow production_compat wildcard routes.
  - Real external side effects remain blocked, fake, guarded, or staging-disabled unless separately approved.
routes:
  - route_pattern: /health
    methods: [GET]
    capability_owner: aicrm_next.platform_foundation
    current_runtime_owner: next
    production_behavior: next_exact
    legacy_fallback_allowed: false
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_runtime_switch_to_next.py
    notes: AI-CRM Next health endpoint from platform_foundation.
  - route_pattern: /api/system/health
    methods: [GET]
    capability_owner: aicrm_next.platform_foundation
    current_runtime_owner: next
    production_behavior: next_exact
    legacy_fallback_allowed: false
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_runtime_switch_to_next.py
    notes: System health endpoint from platform_foundation.
  - route_pattern: /admin
    methods: [GET]
    capability_owner: aicrm_next.frontend_compat
    current_runtime_owner: frontend_compat
    production_behavior: readonly_facade
    legacy_fallback_allowed: false
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_next_admin_ui_data_parity.py
    notes: Admin shell rendered by Next frontend_compat.
  - route_pattern: /admin/customers
    methods: [GET]
    capability_owner: aicrm_next.customer_read_model
    current_runtime_owner: frontend_compat
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Production mode reads PostgreSQL through the customer legacy production facade; fixture/local_contract is not allowed in production.
  - route_pattern: /admin/questionnaires
    methods: [GET]
    capability_owner: aicrm_next.questionnaire
    current_runtime_owner: frontend_compat
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Production mode reads questionnaire PostgreSQL through the legacy production facade; fixture/local_contract is not allowed in production.
  - route_pattern: /admin/questionnaires/new
    methods: [GET]
    capability_owner: aicrm_next.questionnaire
    current_runtime_owner: frontend_compat
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Admin questionnaire editor shell is rendered by Next frontend_compat; production writes remain guarded by questionnaire API contracts.
  - route_pattern: /admin/questionnaires/{questionnaire_id}
    methods: [GET]
    capability_owner: aicrm_next.questionnaire
    current_runtime_owner: frontend_compat
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Admin questionnaire detail/editor page loads production questionnaire detail through the legacy production facade and must not fall through to FastAPI 404.
  - route_pattern: /admin/automation-conversion
    methods: [GET]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: frontend_compat
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_next_production_cutover_readiness.py
    notes: Admin page is Next frontend_compat; write/runtime actions remain guarded separately.
  - route_pattern: /admin/automation-conversion/{path:path}
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Automation conversion detail/setup pages and actions remain legacy-forwarded while the list shell is Next-owned.
  - route_pattern: /admin/jobs
    methods: [GET]
    capability_owner: aicrm_next.admin_jobs
    current_runtime_owner: ai_crm_next
    production_behavior: native_sync_jobs_console
    legacy_fallback_allowed: false
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tests/test_next_admin_jobs_native.py
    notes: Next-native sync jobs console with webhook delivery and broadcast queue operations.
  - route_pattern: /admin/broadcast-jobs
    methods: [GET]
    capability_owner: aicrm_next.admin_jobs
    current_runtime_owner: ai_crm_next
    production_behavior: native_broadcast_queue_console
    legacy_fallback_allowed: false
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tests/test_next_admin_jobs_native.py
    notes: Native queue page over broadcast_jobs and outbound_tasks; approve/cancel operations require action confirmation.
  - route_pattern: /admin/wechat-pay/products
    methods: [GET, POST, PUT, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Production product management uses the retained legacy Flask admin UI for the list entry point.
  - route_pattern: /admin/wechat-pay/products*
    methods: [GET, POST, PUT, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Production product management uses the retained legacy Flask admin UI and product_service for create/edit/list/share while payment/provider side effects remain blocked separately.
  - route_pattern: /admin/wechat-pay/transactions
    methods: [GET]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: frontend_compat
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Admin transaction page is readonly; payment/provider side effects remain blocked.
  - route_pattern: /admin/image-library
    methods: [GET]
    capability_owner: aicrm_next.media_library
    current_runtime_owner: frontend_compat
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Admin media page is readonly; upload adapters remain fake/staging-disabled.
  - route_pattern: /admin/miniprogram-library
    methods: [GET]
    capability_owner: aicrm_next.media_library
    current_runtime_owner: frontend_compat
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Admin miniprogram page is readonly; WeCom media calls remain blocked.
  - route_pattern: /admin/attachment-library
    methods: [GET]
    capability_owner: aicrm_next.media_library
    current_runtime_owner: frontend_compat
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Admin attachment page is readonly; external storage calls remain blocked.
  - route_pattern: /api/customers
    methods: [GET]
    capability_owner: aicrm_next.customer_read_model
    current_runtime_owner: next
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Next exact router; production reads through legacy customer facade when production_data_ready().
  - route_pattern: /api/customers/{external_userid}
    methods: [GET]
    capability_owner: aicrm_next.customer_read_model
    current_runtime_owner: next
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Next exact customer detail route with production facade.
  - route_pattern: /api/customers/{external_userid}/timeline
    methods: [GET]
    capability_owner: aicrm_next.customer_read_model
    current_runtime_owner: next
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Next exact customer timeline route with production facade.
  - route_pattern: /api/messages/{external_userid}/recent
    methods: [GET]
    capability_owner: aicrm_next.customer_read_model
    current_runtime_owner: next
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Next exact recent-message read route; broader /api/messages/* production_compat fallback remains documented below.
  - route_pattern: /api/messages*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.integration_gateway
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: production_compat catch-all for legacy message/archive surfaces until exact Next replacement exists.
  - route_pattern: /api/admin/questionnaires*
    methods: [GET, POST, PUT, DELETE]
    capability_owner: aicrm_next.questionnaire
    current_runtime_owner: next
    production_behavior: guarded_preview
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Read paths use production facade; write/enable/delete/export remain guarded by questionnaire contracts.
  - route_pattern: /api/h5/questionnaires*
    methods: [GET, POST]
    capability_owner: aicrm_next.questionnaire
    current_runtime_owner: next
    production_behavior: guarded_preview
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Public questionnaire read routes are Next exact; submit and diagnostics POSTs forward to legacy Flask so OAuth session identity and production submit behavior are preserved.
  - route_pattern: /api/h5/questionnaires/{slug}/submit
    methods: [POST, OPTIONS]
    capability_owner: aicrm_next.questionnaire
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Public questionnaire submit forwards to legacy Flask so production validation, identity session, external push, and redirect behavior stay intact.
  - route_pattern: /api/h5/questionnaires/{slug}/client-diagnostics
    methods: [POST, OPTIONS]
    capability_owner: aicrm_next.questionnaire
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Public H5 diagnostics POST remains on the legacy transport path.
  - route_pattern: /s/{slug}
    methods: [GET]
    capability_owner: aicrm_next.questionnaire
    current_runtime_owner: next
    production_behavior: guarded_preview
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Public H5 page rendered by Next frontend templates; WeChat auth gate uses the retained legacy OAuth transport and session cookie.
  - route_pattern: /api/h5/wechat/oauth*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.questionnaire
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: OAuth start/callback forward to legacy Flask transport so configured公众号 OAuth redirects to WeChat and writes questionnaire_h5_identity into the legacy session cookie.
  - route_pattern: /api/admin/wecom/tags*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.customer_tags
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Questionnaire editor and WeCom tag management load the real enterprise tag catalog from the legacy production facade until Next owns tag sync and CRUD directly.
  - route_pattern: /api/admin/wecom/tag-groups*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.customer_tags
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: WeCom tag group CRUD remains legacy-forwarded with the same guarded admin tag-management contract.
  - route_pattern: /auth/wecom*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.questionnaire
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: production_compat OAuth-related legacy fallback; not a real OAuth enablement.
  - route_pattern: /api/admin/automation-conversion/reply-monitor*
    methods: [POST, OPTIONS]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: scheduled_safe_mode
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_next_timer_route_readiness.py
    notes: Timer run-due/capture routes require auth and dry-run/noop safety; timers are not enabled here.
  - route_pattern: /api/admin/automation-conversion/jobs/run-due*
    methods: [POST, OPTIONS]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: scheduled_safe_mode
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_next_timer_route_readiness.py
    notes: Automation jobs timer route is safe-mode only.
  - route_pattern: /api/admin/cloud-orchestrator/campaigns/run-due*
    methods: [POST, OPTIONS]
    capability_owner: aicrm_next.ai_assist
    current_runtime_owner: production_compat
    production_behavior: scheduled_safe_mode
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_next_timer_route_readiness.py
    notes: Campaign run-due timer route is safe-mode only.
  - route_pattern: /api/admin/automation-conversion/programs*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Program setup/workspace APIs are still served by the legacy production facade while Next owns the readonly list shell.
  - route_pattern: /api/admin/automation-conversion/profile-segment-templates*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Program operations workspace still loads profile segment template options from the legacy production facade.
  - route_pattern: /api/admin/automation-conversion/agents*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Program operations Agent option/config APIs remain legacy-forwarded during the workspace cutover.
  - route_pattern: /api/admin/automation-conversion/agent-outputs*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Post-Phase 7 standing approval removed only the exact /api/admin/automation-conversion/agent-outputs production_compat decorator after confirming the Next-native GET list route and tests; agent-output subpath fallback, export behavior, wildcard routing, and runtime deletion remain retained.
  - route_pattern: /api/admin/automation-conversion/agent-runs*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Agent run detail APIs remain legacy-forwarded during the workspace cutover.
  - route_pattern: /api/admin/automation-conversion/agent-replay
    methods: [GET, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Agent replay API remains legacy-forwarded during the workspace cutover.
  - route_pattern: /api/admin/automation-conversion/agent-orchestration*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Agent orchestration APIs remain legacy-forwarded during the workspace cutover.
  - route_pattern: /api/admin/automation-conversion/action-templates*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Program operations action-template APIs remain legacy-forwarded during the workspace cutover.
  - route_pattern: /api/admin/automation-conversion/task-groups*
    methods: [GET, POST, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: aicrm_next.automation_engine
    production_behavior: next_native_exact_route
    legacy_fallback_allowed: false
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Post-Phase 7 exact-route cleanup retry removed the selected task-groups production_compat fallback hooks after owner approval, shadow compare, rollback rehearsal, route ownership proof, and exact-entry proof. Rollback restores the two task-groups production_compat decorators only; runtime deletion remains disallowed.
  - route_pattern: /api/admin/automation-conversion/tasks*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Program operation task APIs remain legacy-forwarded during the workspace cutover.
  - route_pattern: /api/admin/automation-conversion/workflows*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Program workflow APIs remain legacy-forwarded during the workspace cutover.
  - route_pattern: /api/admin/automation-conversion/workflow-nodes*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: next
    production_behavior: next_native_verified
    legacy_fallback_allowed: false
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Workflow node list/create/update/delete surfaces are Next-native; broad workflow, task, payment, OAuth, WeCom, timer, outbound, and public-submit fallbacks remain retained separately.
  - route_pattern: /api/admin/automation-conversion/dashboard
    methods: [GET, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Program operations dashboard API remains legacy-forwarded during the workspace cutover.
  - route_pattern: /api/admin/automation-conversion/executions*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Program execution workspace APIs remain legacy-forwarded during the workspace cutover.
  - route_pattern: /api/admin/automation-conversion/execution-items*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Program execution item APIs remain legacy-forwarded during the workspace cutover.
  - route_pattern: /api/admin/automation-conversion*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: next
    production_behavior: guarded_preview
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Next exact readonly/write-preview routes plus production_compat fallback for legacy automation members.
  - route_pattern: /api/customer-automation*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: next
    production_behavior: guarded_preview
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Activation webhook has Next exact route and production_compat fallback; real side effects remain blocked.
  - route_pattern: /api/customers/automation*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.automation_engine
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: production_compat fallback for legacy customer automation surfaces.
  - route_pattern: /api/admin/wechat-pay/products*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Product management APIs forward to legacy Flask product_service so the admin page can create, edit, copy, publish, delete, and generate QR shares.
  - route_pattern: /api/admin/wechat-pay*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: next
    production_behavior: fake_adapter
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Next exact product/transaction routes plus production_compat fallback; real payment provider calls blocked.
  - route_pattern: /api/admin/alipay*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: next
    production_behavior: fake_adapter
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Next exact Alipay transaction routes plus production_compat fallback; real provider calls blocked.
  - route_pattern: /api/products*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Public product API forwards to legacy Flask/product_service so real PostgreSQL product links opened from QR codes do not hit the Next fake commerce repository.
  - route_pattern: /p/{page_slug}
    methods: [GET]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Public product page forwards to legacy Flask/product_service; checkout side effects remain guarded by the legacy payment flow.
  - route_pattern: /pay/{product_code}
    methods: [GET]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Product checkout page forwards to the legacy WeChat-internal H5/JSAPI flow; real provider order creation stays on the legacy guarded /api/h5/wechat-pay* routes.
  - route_pattern: /api/orders*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: next
    production_behavior: fake_adapter
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Order status API exact route plus production_compat fallback.
  - route_pattern: /api/checkout*
    methods: [POST, OPTIONS, GET, PUT, PATCH, DELETE, HEAD]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: next
    production_behavior: fake_adapter
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Checkout routes use fake payment adapter unless a separately approved real provider path exists.
  - route_pattern: /api/wechat-pay*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: next
    production_behavior: fake_adapter
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: WeChat Pay notify/fallback route family; real provider calls blocked.
  - route_pattern: /api/alipay*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: next
    production_behavior: fake_adapter
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Alipay notify/return/fallback route family; real provider calls blocked.
  - route_pattern: /api/h5/wechat-pay*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Legacy H5 WeChat Pay production_compat fallback; real provider calls still blocked.
  - route_pattern: /api/h5/alipay*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.commerce
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_next_production_runtime_gaps.py
    notes: Legacy H5 Alipay production_compat fallback; real provider calls still blocked.
  - route_pattern: /api/admin/image-library*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.media_library
    current_runtime_owner: next
    production_behavior: fake_adapter
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Next exact media routes plus production_compat fallback; real cloud/WeCom media upload blocked.
  - route_pattern: /api/admin/image-library/upload
    methods: [POST, OPTIONS]
    capability_owner: aicrm_next.media_library
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: guarded
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Multipart local image upload used by product slices and image pickers stores image_library rows via legacy PostgreSQL without calling external media providers.
  - route_pattern: /api/admin/attachment-library*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.media_library
    current_runtime_owner: next
    production_behavior: fake_adapter
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Attachment routes use fake/staging-disabled adapters.
  - route_pattern: /api/admin/miniprogram-library*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.media_library
    current_runtime_owner: next
    production_behavior: fake_adapter
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: Miniprogram routes use fake/staging-disabled adapters.
  - route_pattern: /sidebar/bind-mobile
    methods: [GET, HEAD]
    capability_owner: aicrm_next.frontend_compat
    current_runtime_owner: next
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_sidebar_profile_next_owner_readiness.py
    notes: Next exact readonly page owner; mobile binding writes remain guarded by production_compat fallback.
  - route_pattern: /api/sidebar/contact-binding-status
    methods: [GET, HEAD]
    capability_owner: aicrm_next.identity_contact
    current_runtime_owner: next
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_sidebar_profile_next_owner_readiness.py
    notes: Next exact readonly binding status owner; production data failures return degraded/error.
  - route_pattern: /api/sidebar/customer-context
    methods: [GET, HEAD]
    capability_owner: aicrm_next.customer_read_model
    current_runtime_owner: next
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_sidebar_profile_next_owner_readiness.py
    notes: Next exact readonly customer context owner; production data failures return degraded/error.
  - route_pattern: /api/admin/customers/profile
    methods: [GET, HEAD]
    capability_owner: aicrm_next.customer_read_model
    current_runtime_owner: next
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_sidebar_profile_next_owner_readiness.py
    notes: Next exact readonly customer profile owner; production data failures return degraded/error.
  - route_pattern: /api/admin/customers/profile/tags
    methods: [GET, HEAD]
    capability_owner: aicrm_next.customer_read_model
    current_runtime_owner: next
    production_behavior: readonly_facade
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_sidebar_profile_next_owner_readiness.py
    notes: Next exact readonly customer profile tags owner; production data failures return degraded/error.
  - route_pattern: /sidebar*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.frontend_compat
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_next_admin_ui_data_parity.py
    notes: production_compat sidebar fallback retained for admin shell compatibility.
  - route_pattern: /api/sidebar*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.frontend_compat
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_next_admin_ui_data_parity.py
    notes: production_compat sidebar API fallback retained for admin shell compatibility.
  - route_pattern: /api/admin/customers/profile*
    methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD]
    capability_owner: aicrm_next.customer_read_model
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: none
    delete_ready: false
    checker: tools/check_next_admin_ui_data_parity.py
    notes: Customer profile production_compat fallback retained until exact Next owner is complete.
  - route_pattern: /wecom/external-contact/callback
    methods: [GET, POST, OPTIONS, HEAD]
    capability_owner: aicrm_next.integration_gateway
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_next_production_cutover_readiness.py
    notes: Callback route forwards through legacy facade; no new real external behavior is enabled here.
  - route_pattern: /api/wecom/events
    methods: [GET, POST, OPTIONS, HEAD]
    capability_owner: aicrm_next.integration_gateway
    current_runtime_owner: production_compat
    production_behavior: legacy_forward
    legacy_fallback_allowed: true
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_next_production_cutover_readiness.py
    notes: WeCom event callback route forwards through legacy facade; no new real external behavior is enabled here.
  - route_pattern: /mcp
    methods: [GET, POST]
    capability_owner: aicrm_next.integration_gateway
    current_runtime_owner: next
    production_behavior: fake_adapter
    legacy_fallback_allowed: false
    fixture_allowed_in_production: false
    external_side_effect_risk: real_blocked
    delete_ready: false
    checker: tools/check_production_route_resolution.py
    notes: MCP is owned by the aicrm_next.integration_gateway adapter boundary; openclaw_service is not an owner and must not be reintroduced.
