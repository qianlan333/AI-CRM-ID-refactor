api_import_rules:
  api_file_globs:
    - aicrm_next/*/api.py
  forbidden_cross_context_modules:
    - repo
    - service
  allowed_imports: []

forbidden_legacy_markers:
  - legacy_flask
  - openclaw_service
  - forward_to_legacy_flask
  - production_compat

legacy_allowlist:
  - path: aicrm_next/automation_engine/application.py
    rule: forbidden_legacy_marker
    marker: production_compat
    owner: automation_engine
    reason: Historical production-unavailable messages document retired fallback ownership.
    matches:
      - or "profile segment template production repository is not enabled; legacy production_compat fallback remains the production owner.",
      - or "action template production repository is not enabled; legacy production_compat fallback remains the production owner.",
      - or "task group production repository is not enabled; legacy production_compat fallback remains the production owner.",
      - or "workflow production repository is not enabled; legacy production_compat fallback remains the production owner.",
      - or "workflow node production repository is not enabled; legacy production_compat fallback remains the production owner.",
      - or "task production repository is not enabled; legacy production_compat fallback remains the production owner.",
      - or "agent production repository is not enabled; legacy production_compat fallback remains the production owner.",
      - or "agent output production repository is not enabled; legacy production_compat fallback remains the production owner.",
      - or "agent run production repository is not enabled; legacy production_compat fallback remains the production owner.",
  - path: aicrm_next/customer_tags/wecom_tag_live_adapter.py
    rule: forbidden_legacy_marker
    marker: production_compat
    owner: customer_tags
    reason: Historical audit payload field confirms this adapter does not change retired compatibility runtime.
    matches:
      - '"production_compat_changed": False,'
  - path: aicrm_next/integration_gateway/media_live_adapter.py
    rule: forbidden_legacy_marker
    marker: production_compat
    owner: integration_gateway
    reason: Historical audit payload field confirms this adapter does not change retired compatibility runtime.
    matches:
      - '"production_compat_changed": False,'
  - path: aicrm_next/integration_gateway/oauth_identity_adapter.py
    rule: forbidden_legacy_marker
    marker: production_compat
    owner: integration_gateway
    reason: Historical audit payload field confirms this adapter does not change retired compatibility runtime.
    matches:
      - '"production_compat_changed": False,'
  - path: aicrm_next/integration_gateway/oauth_identity_live_adapter.py
    rule: forbidden_legacy_marker
    marker: production_compat
    owner: integration_gateway
    reason: Historical audit payload field confirms this adapter does not change retired compatibility runtime.
    matches:
      - '"production_compat_changed": False,'
  - path: aicrm_next/integration_gateway/openclaw_mcp_ai_assist_live_adapter.py
    rule: forbidden_legacy_marker
    marker: production_compat
    owner: integration_gateway
    reason: Historical audit payload field confirms this adapter does not change retired compatibility runtime.
    matches:
      - '"production_compat_changed": False,'
  - path: aicrm_next/integration_gateway/payment_commerce_live_adapter.py
    rule: forbidden_legacy_marker
    marker: production_compat
    owner: integration_gateway
    reason: Historical audit payload field confirms this adapter does not change retired compatibility runtime.
    matches:
      - '"production_compat_changed": False,'
  - path: aicrm_next/integration_gateway/wecom_contact_callback_adapter.py
    rule: forbidden_legacy_marker
    marker: production_compat
    owner: integration_gateway
    reason: Historical audit payload field confirms this adapter does not change retired compatibility runtime.
    matches:
      - '"production_compat_changed": False,'
  - path: aicrm_next/integration_gateway/wecom_contact_callback_live_adapter.py
    rule: forbidden_legacy_marker
    marker: production_compat
    owner: integration_gateway
    reason: Historical audit payload field confirms this adapter does not change retired compatibility runtime.
    matches:
      - '"production_compat_changed": False,'
  - path: aicrm_next/questionnaire/external_submit_adapter.py
    rule: forbidden_legacy_marker
    marker: production_compat
    owner: questionnaire
    reason: Historical audit payload field confirms this adapter does not change retired compatibility runtime.
    matches:
      - '"production_compat_changed": False,'
  - path: aicrm_next/questionnaire/external_submit_live_adapter.py
    rule: forbidden_legacy_marker
    marker: production_compat
    owner: questionnaire
    reason: Historical audit payload field confirms this adapter does not change retired compatibility runtime.
    matches:
      - '"production_compat_changed": False,'
