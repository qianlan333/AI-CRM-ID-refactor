schema_version: 1
package: aicrm_next

limits:
  max_contexts: 40
  max_cross_context_edges: 191
  max_cyclic_contexts: 19

allowed_cyclic_components:
  - id: legacy_cross_context_runtime_scc
    owner: architecture
    reason: Existing modular-monolith reverse imports must be removed incrementally by the remaining R12 slices.
    remove_by: "2026-08-31"
    contexts:
      - admin_auth
      - admin_jobs
      - admin_shell
      - automation_engine
      - channel_entry
      - cloud_orchestrator
      - commerce
      - customer_read_model
      - customer_tags
      - external_push
      - identity_contact
      - integration_gateway
      - media_library
      - message_archive
      - platform_foundation
      - public_product
      - questionnaire
      - send_content
      - service_period
